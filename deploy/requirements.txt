litellm[proxy]==1.98.0
prisma
fastmcp>=3.4,<4
rdflib>=7.6
owlrl
openpyxl
streamlit>=1.60
redis>=5
psycopg[binary]>=3.2
opentelemetry-sdk
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation-asgi
opentelemetry-instrumentation-urllib
# Microsoft Agent Framework — ONLY the two packages we import (agent_framework + agent_framework.openai).
# NOT the `agent-framework` meta / `[all]` extras: those pull loosely-pinned PRERELEASE sub-packages
# (devui, ag-ui, azure, …) that make pip backtrack forever and fail the container build.
agent-framework-core==1.16.0    # `agent_framework`: Agent, WorkflowBuilder, MCP tools, ChatOptions
agent-framework-openai==1.14.1  # `agent_framework.openai`: OpenAIChatClient
mcp>=1.29                        # MCP SDK — agent_framework exports MCPStreamableHTTPTool from it (not pulled by fastmcp-slim)
vsdx>=0.6                        # read Visio .vsdx diagrams (visio-to-archimate BA ingest)
jsonschema>=4                   # validate the BA-output contract
msal>=1.31                      # agent identity: Entra client-credentials (shared/identity.py)
