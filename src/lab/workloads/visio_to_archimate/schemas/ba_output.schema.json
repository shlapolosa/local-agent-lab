{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://local-agent-lab/schemas/ba_output.schema.json",
  "title": "Business Analyst system description",
  "description": "The typed contract the Business Analyst agent emits from a parsed Visio diagram and hands to the Architect agent. A plain-language description of the system with a candidate ArchiMate classification per element; the Architect validates and formalises it. Grouping (actors/components/data/behaviors) is a BA organising aid; layer+aspect+candidateType is what the Architect consumes.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "systemName",
    "summary",
    "actors",
    "components",
    "data",
    "behaviors",
    "relationships",
    "openQuestions"
  ],
  "properties": {
    "systemName": {
      "type": "string",
      "minLength": 1,
      "description": "Short name of the system the diagram depicts."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "One or two sentences: what the system is and does, in plain language."
    },
    "actors": {
      "type": "array",
      "description": "Who or what acts on the system: people, roles, external systems (business/active structure).",
      "items": {
        "$ref": "#/definitions/element"
      }
    },
    "components": {
      "type": "array",
      "description": "The moving parts: application components, services, interfaces, nodes, system software (active structure + the services/interfaces they expose).",
      "items": {
        "$ref": "#/definitions/element"
      }
    },
    "data": {
      "type": "array",
      "description": "What the system holds or moves: data objects, artifacts, information (passive structure).",
      "items": {
        "$ref": "#/definitions/element"
      }
    },
    "behaviors": {
      "type": "array",
      "description": "What happens: processes, functions, events, services (behaviour aspect).",
      "items": {
        "$ref": "#/definitions/element"
      }
    },
    "relationships": {
      "type": "array",
      "description": "Directed dependencies between named elements, with the BA's reading of intent. Direction matters downstream (server->served, whole->part, realizer->realized).",
      "items": {
        "$ref": "#/definitions/relationship"
      }
    },
    "openQuestions": {
      "type": "array",
      "description": "What the diagram does not settle: ambiguous shapes, missing types, contradictions between caption and stencil, orphans. Empty array if none.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "definitions": {
    "element": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "name",
        "role",
        "layer",
        "aspect",
        "candidateType"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "The element's caption / identity, as read from the diagram."
        },
        "role": {
          "type": "string",
          "minLength": 1,
          "description": "Plain-language description of what this element is and does in the system."
        },
        "layer": {
          "type": "string",
          "enum": [
            "Motivation",
            "Strategy",
            "Business",
            "Application",
            "Technology",
            "Implementation",
            "Physical"
          ],
          "description": "ArchiMate layer the BA proposes for this element."
        },
        "aspect": {
          "type": "string",
          "enum": [
            "active",
            "behaviour",
            "passive",
            "motivation"
          ],
          "description": "ArchiMate aspect: active structure, behaviour, passive structure, or motivation."
        },
        "candidateType": {
          "type": "string",
          "minLength": 1,
          "description": "The BA's best-guess exact ArchiMate 3.1 type name (e.g. ApplicationComponent, ApplicationInterface, Node, DataObject, BusinessActor). A proposal; the Architect validates it against the semantic layer."
        },
        "sourceShapeIds": {
          "type": "array",
          "description": "Optional: ids of the Visio shapes this element was read from, for traceability.",
          "items": {
            "type": "string"
          }
        },
        "provenance": {
          "description": "Where this element came from and how it was read. `source` is the input kind (a diagram shape, a design/requirements document); `representation` is how it was interpreted (the deterministic parse, vision over an image, or extracted document text). REQUIRED on every element by the deterministic gate that follows the BA (the schema also accepts the bare-string shorthand naming only the representation, which the gate expands to {representation: <s>}).",
          "oneOf": [
            {
              "type": "string",
              "enum": [
                "structure",
                "vision",
                "document"
              ],
              "description": "Shorthand: the representation only. The gate expands it to the full object, filling `source` with the only kind that representation can come from (structure/vision -> diagram, document -> document)."
            },
            {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "source": {
                  "type": "string",
                  "enum": [
                    "diagram",
                    "document",
                    "requirements"
                  ],
                  "description": "Input kind the element was read from."
                },
                "representation": {
                  "type": "string",
                  "enum": [
                    "structure",
                    "vision",
                    "document"
                  ],
                  "description": "How the source was interpreted: deterministic structure, vision over an image, or document text."
                }
              },
              "required": [
                "source",
                "representation"
              ],
              "description": "Where the element came from and how it was read. Both fields are required in this form: a half-filled provenance says nothing."
            }
          ]
        }
      }
    },
    "relationship": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "from",
        "to",
        "type",
        "intent"
      ],
      "properties": {
        "from": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the source element (must match an element name declared above)."
        },
        "to": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the target element (must match an element name declared above)."
        },
        "type": {
          "type": "string",
          "description": "Candidate ArchiMate relationship: Composition, Aggregation, Assignment, Realization, Serving, Access, Influence, Triggering, Flow, Specialization, or Association. The Architect validates legality against the semantic matrix.",
          "enum": [
            "Composition",
            "Aggregation",
            "Assignment",
            "Realization",
            "Serving",
            "Access",
            "Influence",
            "Triggering",
            "Flow",
            "Specialization",
            "Association"
          ]
        },
        "intent": {
          "type": "string",
          "minLength": 1,
          "description": "Plain-language reading of the dependency (e.g. 'the gateway exposes the /v1 interface', 'the agent is served by the gateway')."
        }
      }
    }
  }
}
