{
  "type": "object",
  "properties": {
    "build_amount": {
      "type": "number",
      "minimum": 0,
      "description": "The build cost captured at intake, if one was \u2014 never invented."
    },
    "build_provenance": {
      "enum": [
        "vendor quote",
        "budget bucket",
        "estimate",
        ""
      ],
      "description": "What the captured figure IS. Empty when nothing was captured."
    },
    "build_basis": {
      "type": "string",
      "description": "Where in the intake the figure came from."
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Anything the design needs that is not a catalogue component and so will not be in the join \u2014 named, for the Review Board."
    }
  },
  "required": [
    "build_provenance",
    "notes"
  ],
  "additionalProperties": false,
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "cost_inputs"
}
